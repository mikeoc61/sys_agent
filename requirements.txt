# Fallback for users who don't have uv. The script's authoritative dependency
# list is the PEP 723 inline block at the top of sys_agent.py — keep this
# file in sync if you bump versions there.
openai>=3.19
anthropic>=1.8,<2.0

# Real GNU readline on every POSIX host, replacing libedit where the
# interpreter links it, so colored prompts render correctly and Up/Down
# history navigation doesn't overwrite the prompt prefix. The PEP 508 marker
# skips Windows, which has no readline to replace.
gnureadline>=8.1; sys_platform != 'win32'
